// divide a by b
module divider (
    input a[32], // dividend
    input b[32], // divisor
    output d[32] // a/b
  ) {

  always {
        if (b != 0){
            d = a/b;
        } else {
            d = 0
        } 
  }
}